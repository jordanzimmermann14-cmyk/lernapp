# Lernfortschritt-Tracker

## Starten
```bash
npm install
npm run dev
```
Danach im Browser die angezeigte Adresse öffnen (meist http://localhost:5173).

Der Fortschritt wird im localStorage deines Browsers gespeichert (pro Browser/Gerät).
